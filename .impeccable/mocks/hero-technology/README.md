STATUS: Rejected by the owner. The abstract element is unrelated to the agent/brand and interferes with reading. The owner now directs the hero toward DataFast-style product clarity.

# Unselected hero direction

The owner rejected the white centered task-panel hero during its finish review. That review was interrupted and has no ship verdict. The explicit portrait-color request is implemented and remains in effect.

A new optional direction question was sent: technology / typography / personal portrait. No answer has arrived. The technology prototype is an exploratory direction, NOT an approved implementation. Public Resend and basement were viewed in Brave to calibrate a single large dimensional form on a dark field. Linear was also viewed but its app-preview structure was not carried forward.

Live prototype: http://127.0.0.1:3730/ served from /tmp/karchx-hero-sept8/concepts via Python server. Source copy: preview.html. It resolves local copies of installed Three.js, RoomEnvironment, two existing Golos fonts and the existing KARCHX logo from the temporary server directory. No third-party site artwork or copy. The TorusKnotGeometry is a live procedural WebGL form, not a generated raster or a product screen. Motion finishes after a short entrance, reacts to mouse position and remains static under reduced motion. It pauses in hidden tabs.

Brave checks at1440×1000 and390×844: WebGL renders, no overflow, CTA in viewport. Screenshots are desktop.png and mobile.png. This is an exploration, not production validation. The prototype's simplified navigation and chrome do not authorize removing locale navigation from the actual site.

Await a concrete direction preference before replacing the main hero again. The current main site still contains the rejected task-panel variant, and DESIGN.md still describes the preceding accepted system. Update UI and docs only after resolving the direction, then use fresh finishing handoffs. Original photo colors are the settled change.
