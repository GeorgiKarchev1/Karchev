## verdict

1. Resolved — CRM label contrast. Fresh PNGs retain the rust label on its cream tile; `app/studio.css` and computed evidence confirm `#85542f` on `#f4e5d5`, measuring **5.15:1**, above the required **4.5:1**. All eight full-page recaptures and the cookie capture remain valid at their original paths and expected dimensions.
2. Direct regressions — none observed. The correction preserves the diagram's hierarchy and palette; supplied checks report loaded images, zero page errors, and zero overflow across both languages and all four widths.

## remaining

Clear. This ship disposition covers the scored contrast correction and its direct regressions, not a new review of the whole surface.

disposition: ship

